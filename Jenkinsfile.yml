pipeline {
  agent any
 
  tools {nodejs 'nodeJs'}
 
  stages {
  
   
    
    stage('Install Dependencies') {
      steps {
        sh 'npm install'
      }
    }
    
    stage ('Run') {
      steps {
      
        sh 'npm run test ./tests'  
      }
    }
  
  
  }
}
