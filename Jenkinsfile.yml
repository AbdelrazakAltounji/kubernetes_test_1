pipeline {
  agent any
 
  tools {nodejs "nodeJS"}
 
  stages {
    stage('Clonning') {
      steps {
        git clone 'https://github.com/AbdelrazakAltounji/kubernetes_test_1.git'
      }
    }
    stage('Install Dependencies') {
      steps {
        sh 'npm install'
      }
    }
    
    stage ('Run') {
      steps {
      
        sh 'npm run test ./tests/google'  
      }
    }
  
  
  }
}
