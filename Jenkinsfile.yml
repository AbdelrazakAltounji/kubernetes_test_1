pipeline {
  agent any
        
 
 
  tools {nodejs 'nodeJs'}
 
  stages {
  
   stage('Clone Git repo'){
    steps {
       git 'https://github.com/AbdelrazakAltounji/kubernetes_test_1'
       }
     }
    
    stage('Install Dependencies') {
      steps {
        sh 'npm install'
      }
    }
    
    stage ('Run') {
      steps {
      
        sh 'npm run test ./tests'  
      }
    }
  
  
  }
}
