pipeline {
  agent any
 
  tools {nodejs 'NodeJS'}
 
  stages {
  
    stage('Git Clonning') {
      steps {
        git 'https://github.com/AbdelrazakAltounji/kubernetes_test_1.git'
      }
    }
    
    stage('Install Dependencies') {
      steps {
        sh 'npm install'
      }
    }
    
    stage ('Run') {
      steps {
      
        sh 'npm run test ./tests/google'  
      }
    }
  
  
  }
}
