pipeline {
  agent any
        
 docker { image 'nginx:latest' }
      
 
  tools {nodejs 'nodeJs'}
 
  stages {
  
   
    
    stage('Install Dependencies') {
      steps {
        sh 'npm install'
      }
    }
    
    stage ('Run') {
      steps {
      
        sh 'npm run test ./tests'  
      }
    }
  
  
  }
}
